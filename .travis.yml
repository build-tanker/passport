language: go

services:
  - docker

go:
  - 1.9.x

before_install:

jobs:
  include:
    - stage: test binary
      script:
        - make build_fresh
        - bash <(curl -s https://codecov.io/bash)
    - stage: push container
      script:
        - make update
        - make compile
        - make deploy_quay

after_success: